node_modules/
/vault/
dist/
node-compile-cache/
tsx-1000/

# Agent tooling scratch — machine-local, never shared
.claude/
.seeds/
# A malformed path from the deck-curation run; its output lives in data/decks/
local:/

# Embedding evals — derived vectors, rebuildable (Q-3)
/data/eval-007/
/data/eval-053/
