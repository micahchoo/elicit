# Harvest eval — ticket 037, against 295 hand-marked cuts

Ticket 037 says the harvester drops episodes and mislabels facet and stance.
`docs/ingest-triage-2026-08-02.md` is the first labelled set large enough to
check that: 295 real proposals over nine years of Micah's published writing,
each marked keep or drop with a reason code, produced by the live clerk model
on the real harvest path. This file records what the harvester did against that
set before ticket 037, what changed, and what did not.

Two measurements are reported and they are not the same kind of thing.

**Offline** replays the frozen 295 cuts through the gates. It costs nothing,
repeats exactly, and answers every question about admissibility.

**Live** is a 19-turn sample of the same corpus, run twice against
`qwen3.6:35b` — once with the old system prompt, once with the new one. It is
the only way to see facet and stance at all, because the review file recorded
cut text and nothing else. 19 of the 59 corpus turns, chosen to cover the
capstone's field notes, the burnout passage, the two quotation-heavy turns, and
five whole small posts.

## The set, and one correction to it

The triage doc's own arithmetic is off by one in two places, and the code is
the fact:

- The capstone's `log` list is headed "(49)" and contains 50 numbers.
- Capstone cut **86** ("It was heartwarming to see that they were so excited to
  help even though they didn't live in Pune") carries no reason code at all. It
  is inside the 149 total drops and outside every code list.

Neither changes a conclusion. Both are recorded so the next person to load the
file does not spend twenty minutes finding them again.

## Before: the 044 gate is inert on prose

`src/harvester/admissibility.ts` rejected **0 of 295** cuts. Not 0 of the hard
ones — 0 of all of them, including all 9 the triage marked `frag`, which is the
exact class the ticket predicted it should have caught.

That is not a bug in the gate. The gate was built for conversation, against
"Yes.", "dunno" and "This question makes no sense." Nothing in nine years of
published prose looks like that. The gate is inert here because prose does not
contain the failure it was written to stop.

## What the labels say is actually wrong

| reason | n | share of the 149 drops |
|---|---|---|
| `world` — true regardless of who wrote it | 56 | 38% |
| `log` — activity record, no stance | 58 | 39% |
| `spec` — product description | 12 | 8% |
| `scaffold` — talk about the document | 13 | 9% |
| `frag` — not a standalone proposition | 9 | 6% |
| `theirs` — attributed to someone else | 2 | 1% |
| (uncoded) | 1 | — |

Plus 7 excluded under Q-51 as another person's words.

`world` and `log` are 76% of the problem. `frag` is 6%.

## The gate that is not here

The criterion Micah used is one sentence: *would this be evidence about the
person if you did not know who wrote it?* Six candidate mechanizations were
measured against all 295 cuts:

| predicate | catches of 114 `world`+`log` | keeps destroyed | precision |
|---|---|---|---|
| no first-person singular | 76 | 53 | 48% |
| no first-person and no stance verb | 32 | 16 | 54% |
| past-tense action verb, no stance verb | 37 | 14 | 69% |
| third-person and past-tense action | 20 | 4 | 74% |

Calling every cut junk already scores 53%, because 53% of them are. The best
rule buys 21 points of precision and pays in Micah's own sentences.

The rules are not crude; the distinction is not structural. "The workshop space
is where five brothers work from" (drop) and "Fragility is an honest and
important understanding because it changes the way we look at incentives"
(keep) are both third-person declaratives. "I made a rough spreadsheet which I
shared with Padmini" (drop) and "I started to play with the idea of researching
through doing" (keep) are both first-person past. One reports an action, the
other reports a stance. No regex sees that, because the difference is what the
sentence is *about*.

**So this one is not mechanizable, and nothing was shipped for it.** The
judgement stays with the person, in review. It also must not be handed to a
model call: that is the self-report the admissibility gate exists to route
around, and it would be a gate nobody had evaluated. The absence is recorded in
`src/harvester/admissibility.ts` with the numbers, so the next person does not
re-derive it from first principles.

## What was shipped, and what it measures

### 1. Q-51 at cut level, inside the harvester

`scripts/ingest-posts.ts` already had `isQuotedFromSource`, and it worked — but
only in the importer, and only after the review file had already been written
with seven of Annemarie Mol's and Sara Ahmed's sentences in it as Micah's
prose. The same predicate, applied to the *turn* the harvester was handed
rather than to the source file:

**7 of 7 quoted cuts caught. 0 false positives on the other 288.**

It is now `admissible(text, { source: turn.text })`. Every harvest path gets it,
not just the importer. Q-1 is untouched: the rule only ever subtracts, and the
exact-substring check still stands on its own.

### 2. A structural standalone gate — mid-sentence cuts become Buds

Eval finding #6 says `standalone` is the model grading its own homework, and
ticket 035 closed without the structural check it promised. Two candidate
checks, measured on the same 295:

| check | catches of 9 `frag` | keeps caught |
|---|---|---|
| leading bare pronoun or demonstrative (035's proposal) | **0** | **25** |
| starts on a lowercase letter (shipped) | 7 | 4 |

035's rule is a keep shredder. Real prose opens sentences with expletive "It
was interesting to see…" and discourse "This modularity would allow…"
constantly; all 25 hits are Micah's keeps and none is a fragment. **Do not
implement it as written.**

The lowercase test ships, and it routes to the **Bud** path rather than
dropping. A Bud keeps the fragment and can be asked about, so its four false
positives cost a delay, not a loss. The two `frag` cuts it misses are full
grammatical sentences with dangling referents ("…I came across the biggest
issue." — which issue?). No structural test reaches those.

### 3. Facet and stance read off the text where the text says so

- **`superseded`** never fired in either eval, including on "I used to think X.
  I no longer think that." The markers now force the stance whatever the model
  said. `used to` requires a mental verb behind it, so "I used to work at the
  shop" stays habitual past.
- **`intention`** was applied to 5 of ~14 cuts and correct on none — the
  model's don't-know bucket. A cut labelled `intention` with no want, plan or
  goal in the person's own words is now **counted and left alone**. The marker
  proves the label is wrong and says nothing about which of the seven other
  facets is right, and a wrong label swapped for a different wrong label is not
  a fix. The prompt is the fix; the counter is how anyone knows whether it
  worked.
- **`episode`** cannot be corrected after the fact, because the failure is a cut
  that was never proposed. `episodeAnchoredTurns` and `episodeBlindTurns` count
  turns that name when something happened and yielded no episode cut. Shadow
  measurement, per Q-35.

### 4. The prompt

Facet and stance were bare enum values. Each now carries the test that
separates it from its neighbours, `episode` is named as the priority when a
turn holds both an occasion and the belief drawn from it, the supersession
markers are spelled out, and cuts are told to start and stop on sentence
boundaries.

## Offline before and after, over all 295

| | proposals | of which Micah kept | precision |
|---|---|---|---|
| before (044 gate only) | 295 | 139 | 47.1% |
| after (037 gates) | 276 | 135 | 48.9% |

Nineteen cuts left the proposal stream: 7 dropped as quotations, 12 routed to
Buds. **Zero keeps were destroyed.** Four keeps were delayed to the Bud path.

The gain is small and it is honest: `world` and `log` are three quarters of the
junk and nothing here touches them, because nothing structural can.

## Live before and after

Facet and stance are not in the review file, so they need model calls. 19 of
the 59 corpus turns were run against `qwen3.6:35b` with the old prompt (105
cuts, 1332s), then 12 of those 19 were repeated with the new one (74 cuts,
1165s). Every comparison below is over the same 12 turns. Total live cost is
about 60% of one full dry run. Every chunk parsed as clean JSON in both runs —
the longer prompt did not collapse the model, which was the risk.

| | before | after |
|---|---|---|
| raw cuts from 12 turns | 71 | 74 |
| fabricated | 0 | 0 |
| quoted cuts proposed (Q-51) | 3 | **0** |
| facet or stance outside the vocabulary | 3 | **0** |
| cuts starting mid-sentence | 1 | **0** |
| `intention` cuts | 8 (13%) — 6 with no want, plan or goal in the words | **0** |
| `episode` cuts | 4 (6%) | **22 (30%)** |
| dated turns yielding no episode cut | 2 of 3 | **0 of 3** |
| matched proposals Micah marked keep | 27 of 50 (54%) | **30 of 51 (59%)** |

### The ticket's headline claim is true, and the prompt is what fixes it

Episode under-production is real at corpus scale, not just in one sitting: 4
episode cuts of 71, and two of the three turns that name when something
happened produced none. Ticket 037's own evidence update said chunking had
already fixed this. On six turns it looked fixed. On twelve it is not.

Naming `episode` as the priority moved it from 4 cuts to 22, and the 22 are
episodic — "Jahnavi and I went to the workshop of the Kumbhara potters", "Back
in 2021, I experienced how arbitrary and niche the process of creating a
panable, zoomable and annotable image was", "After seminar 2 when it became
clear to me that I couldn't go back to Bidar because of COVID, I stopped
working on the project completely". That last one is Micah's keep 135, the
burnout, and it is the exact shape the ticket describes: the dateable event
under the self-theory, previously not cut as one.

A limit worth stating: some of the new episode cuts are material Micah drops as
`log`. The facet is right and the keep is still wrong, because facet and
keep-worthiness are different questions. Fixing the label does not fix the
selection.

### `intention` was the model's don't-know bucket, and defining it emptied it

Eval finding #7 saw `intention` on 5 of ~14 cuts, correct on none. On eight
times the sample the pattern held: 8 of 71 cuts, and six of the eight state no
want, plan or goal anywhere in Micah's own words. After the facet definitions
went into the prompt, `intention` was used **zero** times in 74 cuts. The
volitional-marker counter now has nothing to count, which is the outcome to
want from a counter.

### The one live supersession case in nine years of writing

`blog/the-disenfranchisement-of-adivasis-in-kerala` is one turn and holds the
corpus's only supersession marker. Both prompts, same turn:

```
### before (5 cuts)
  intention        commitment           I would design better systems for them to inhabit.
  fact             self-observation     I was starting to see the kind of bubbles that I was living in
  causal-theory    report-of-fact       the invisibility of caste is its biggest barrier to annihilati
  fact             self-observation     my assumptions about designing solutions for them were presump
  causal-theory    report-of-fact       the trap lies in the hero-worship that design engages.

### after (5 cuts)
  episode          avowal               It was then that I decided (very naively) that I would design
  lifetime-period  self-observation     By this point in my research, I was starting to see the kind o
  construct        avowal               I also gained perspective on how the invisibility of caste is
  episode          avowal               After reading the histories of the Paniyas, I understood that
  construct        superseded           I understand now that the trap lies in the hero-worship that d
```

Five fragments with a fallback label became five whole sentences with the
ticket's missing stance on the right one. Three of the five "before" cuts start
mid-sentence and would now be held as Buds.

It also shows the limit of the structural override. The old run cut "the trap
lies in the hero-worship that design engages." — severing "I understand now"
from the sentence. No marker survives in that cut, so no post-check could have
rescued it. **A structural stance check can only read markers the cut still
contains**, which is a second reason to make cuts stop and start on sentences.

The override is therefore unit-tested and, on this corpus, never needed: the
model reached `superseded` on its own once the markers were named. It stays
because the prompt is a request and the check is not.

## What was measured and deliberately left alone

- **`world` and `log`** — 76% of the junk, not mechanizable, argued above.
- **The facet of a mislabelled `intention`** — the marker proves the label
  wrong and cannot say what is right.
- **Straight-quote detection for Q-51** — the curly-quote rule is the one
  measured at 0 false positives. Straight quotes do duty for scare quotes,
  coinages and titles; extending to them is unmeasured and would eat "the
  \"internal wall\"", which is Micah's own term in Micah's own sentence.
- **Cut granularity by length** — 037 suggests a minimum cut size. The triage
  says no: glitch-art's single keep is eleven words, and the review notes that
  "any minimum-length filter would drop this silently".

## Reproducing this

The harness lives outside the repo, in `/tmp/t037`, because it is a
measurement and not a product. What it needs is all here: the 295 cuts are in
`docs/ingest-review-2026-08-02.md` under frozen numbering, the marks are in
`docs/ingest-triage-2026-08-02.md`, and the turn reconstruction is the
selection logic in `scripts/ingest-posts.ts` — replaying it reproduces all 59
turns exactly, with all 295 cuts as substrings of their own turn.

