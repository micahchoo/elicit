# Elicit — Decision Register

Stable IDs for locked design decisions. Source documents: `CONTEXT.md`
(glossary + invariants), `docs/adr/0001-local-models-only.md`,
`docs/interface-references.md`, `research-shape-of-the-problem.md`.

| ID | Decision | Source |
|----|----------|--------|
| Q-115 | Thread-aware queue draw (148 reopened): the strike unit is the SITTING, persisted in queue-engagement.json. One pivoted-away queue-opener reply = one strike per sitting; two consecutive strike-sittings pause ALL queue draws (bank/imported carry the sitting) for a doubling cooldown of sittings (2, 4, 8-cap); the first post-cooldown draw is the probe; any engaged reply resets. Measured on the archived 20-serve contamination: 15/20 blocked (was 0/20 per-snippet). Per-thread deferral kept beneath it. | Ruling (Micah, 2026-08-06, "per sitting") |
| Q-114 | Headless idempotency (145 remainder): a mutating /api or /v2 request carrying an `Idempotency-Key` header runs once; repeats of the key — including one arriving while the first is in flight — replay the first response (`idempotency-replayed: true`). Opt-in, route-scoped, in-memory (30 min, capped); the browser never sends the header. | Ruling (Micah, 2026-08-06, "4 fix") |
| Q-113 | The vault asks who it is about: name + pronouns, optional, at setup and once on the home screen (`POST /api/profile`; stored beside the vault as configuration, never as evidence — Q-1). With a profile, harvest readings and wiki claim bodies refer to the person by name or pronoun, varying naturally, never as "the user"; profile words join the coach clustering's frame-word exclusions so the name cannot become the mega-cluster the "User" artifact was. | Ruling (Micah, 2026-08-06, "ask the user their name and pronouns, use it randomly") |
| Q-112 | Declining a seeded coach offer PARKS it behind an evidence bar: the cluster may re-offer only after 3 genuinely new claims join it (provisional, set equal to the Q-111 mint threshold — the cluster re-earns itself), and the re-offer is logged. Not archive-forever, not free re-minting. | Ruling (Micah, 2026-08-05, coach grill Q3) |
| Q-111 | Direction seed threshold: 3+ claims clustering on a theme, regardless of sitting spread, mints an un-coached DirectionRecord; NO cap on waiting candidates. Fires sooner for a new user; the fake-theme risk is accepted with eyes open (146's fusion/dedup is the mitigation). Every evaluation logs the cluster sizes it saw, for honest re-tuning later. | Ruling (Micah, 2026-08-05, coach grill Q2) |
| Q-110 | The coach bootstraps through BOTH doors: (1) docket-seeded — claims clustering on a theme mint an un-coached DirectionRecord, licensing a passive offer on the waiting surface (offer-only ⇒ ships live with logged evaluations, Q-62); (2) user-declared — a "make this a direction" verb on wiki claims. Accepting an offer remains the only act that makes a Direction coached. Breaks the recorded circularity (63 evaluations, directions=0: offers required records only a prior offer could create). | Ruling (Micah, 2026-08-05, coach grill Q1) |
| Q-109 | Repair press confirms in-idiom: pressing `not mine` expands the margin line once to `unlink · keep` — same turn, no dialog, no model; `unlink` executes the repair, `keep` collapses it, both words spent either way. Needed because Q-108 makes repairs permanent. | Ruling (Micah, 2026-08-05, repair-verb grill Q6) |
| Q-108 | Repair visibility is the activity stream line only — no repairs list, no restore verb; a pressed repair is permanent. The transcript's template turn (Q-106) and the stream line are the full audit trail. | Ruling (Micah, 2026-08-05, repair-verb grill Q5) |
| Q-107 | Correction turns are meta — never harvested. A turn accompanying a pressed `not mine` (or one the model flagged as a prose dispute) is excluded from harvest proposals entirely; real material inside a correction re-enters via re-telling or Restatement, both the user's hand. Kills the eval anti-pattern where "that is not my story" was read as "the user draws a boundary around their personal narrative." | Ruling (Micah, 2026-08-05, repair-verb grill Q4) |
| Q-106 | Quarantine is blunt, snippet-level premise revocation: a repair writes a side-record (`repairs/` in the vault: snippetId@version, quotedFragment, sitting, at) consulted at every draw point — resonance, juxtaposition, composed minting, queue draw. The snippet's text stays untouched in the archive (lineage is what happened); open queue entries citing it expire immediately; wiki claims citing it are surfaced "touched by a repair — review" in the wiki review area but no status moves without the user. No thread-scoped variant: repair may not depend on a fuzzy thread key. | Ruling (Micah, 2026-08-05, repair-verb grill Q3) |
| Q-105 | The repair turn is a fixed mechanical template, on the record in the transcript: acknowledges the error, names the severed link abstractly (never re-quotes the disputed fragment — quoting a correction back reads as arguing), promises it won't return, and carries a fresh question from a DIFFERENT thread (bank/imported, never a callback) in the same turn. No LLM composes any of it. | Ruling (Micah, 2026-08-05, repair-verb grill Q2) |
| Q-104 | Correction detection: a mechanical `not mine` margin verb on any question carrying a quotedFragment is the ONLY mechanism with consequences. Model-detected prose disputes ("that is not my story") never quarantine anything — they oblige the next turn to acknowledge and point at the verb. Rationale: the eval's worst moments were the model freelancing on ambiguous signals; repair must be the one move that can't misfire, and nothing automated ever destroys a true link. | Ruling (Micah, 2026-08-05, repair-verb grill Q1) |
| Q-103 | Dossier battery v2 — sparse canon, drafted and ratified in one ruling (executes Q-102 early: its "current battery finishes under the mitigation" premise broke when cycle-3 attempt 3 voided on the third canon-fidelity variant). Every dossier gains a `canon:` frontmatter list — at most ten atomic fixed facts, each conjunctive (relationship + event + place/time) with distinctive vocabulary; the Life story section names the list as fixed points and licenses everything else as open ground; diachronic cues get several entry doors weighted toward reliably-visited ground (family, home, identity) and cue texts carry their own discriminating markers; persona prompt binds the canon list with cue-level force (src/loop/persona.ts). Grounds: personas hold frontmatter-level facts but improvise substitutes for dense prose (c03: scripted brother became a best friend in one arm, an unrelated patient in the other, plus cross-cue contamination — brother described alive against c1). Audit corollary: marker sets must verify the referent, not a token — single common words false-pass. Spec amended (docs/loop-dossier-spec.md, "Sparse canon"); battery re-freezes at this commit. | Ruling (Micah, 2026-08-04: "draft and ratify") |
| Q-102 | Dossier tensions move to reliably-visited ground: future dossier revisions plant their central tensions (revision cues included) in material the topic engine demonstrably visits in a fresh-vault life — family, home, identity — not in domains it may never open (career starved across 11 straight cycle-3 sittings). Applied BETWEEN cycles only, never to a battery mid-cycle (Q-94 freeze holds during a run); the current battery finishes under the persona-contract mitigation (2514523: scripted cue facts are verbatim canon, cover story on record by sitting 2, revision corrects the told story at its scripted sitting). | Ruling (Micah, 2026-08-04: "go with your recs on dossier") |
| Q-101 | Session-3 apparatus rules ratified into docs/loop-prompt.md: one dispatch = one sitting with a symmetric fixed inter-sitting gap (90s both arms); a daemonized driver process owns each arm's timing loop (crons observe and alert, never wait-then-act); each life is audited for its dossier's planted from/to markers before the next arm launches, and a passing check still gets a human-read of the transcripts. Supersedes the shakedown-era full-life-per-dispatch guidance. Costs that bought them: c02 void (pacing asymmetry), two c03 silent stalls (notification-hop timing), c03 attempt-2 fact substitution (generic check false-pass). | Ruling (Micah, 2026-08-04: "ratify the prompt") |
| Q-100 | The clerk model is `gemma4:e4b`, replacing `qwen3.6:35b`, for the owner's instance and eval trials alike (Q-93 fidelity: eval never runs a clerk the product does not). Ground: same-workload probe 2026-08-04 — end-of-sitting to harvest proposals 25s vs 5m35s (13.4x), warm per-call 10.7s vs 22.9s, clean JSON 3/3 both, extraction coverage equal or better (11 vs 9 proposals; gemma kept a claim qwen dropped; facet labels comparable). Supersedes the "far slower per call" clerk note in Q-77-era prose and ADR-0001's first observation; qwen's latency is what saturated the shakedown's clerk, confounded cycle-1 harvest, and starved cycle-2's candidate arm. Evidence: eval/instances/loop/clerk-probe/. | Ruling (Micah, 2026-08-04: "lets change it for the instance and the trials and restart the trials") |
| Q-99 | Graduation is DATA-DRIVEN, symmetric with demotion: `data/graduations.json` consulted at read time through `isLive` (src/loop/graduations.ts), written by the loop at landing beside its graduation ledger line. The register's `live:` flag and its pin tests keep guarding what SHIPS; the owner's instance carries its own evolved state; demotion beats graduation at the read; the store has no removal API (the visible reverse of a graduation is a demotion). Amends the enforcement of Q-89 ("graduates alone" — now true without editing tests) and extends Q-97's writable set with `data/graduations.json`. Session-1 finding: every shadow threshold is pin-tested, so code-flip graduation collided with the add-only-tests rule; this is resolution (b). | Session-1 ruling (Micah, 2026-08-04: "2 and go") |
| Q-98 | The scoring rubric is ratified and FROZEN (`docs/loop-rubric.md`): the judge is a fresh agent with the SAME dossier reading both archived lives, blind-labeled first/second in randomized recorded order; verdicts on FIVE lived surfaces (questioning, harvest, wiki, descents, returns); the disconfirming set is closed (got-wrong, compliance, flattened, unconsented, forgotten) and satisfaction language is BANNED from the rubric; behavioral traces are harness-computed companions, never sufficient alone. Keep rule: constitution-gate void first, then ≥1 resolving-cited win AND 0 resolving-cited regressions across 5×5 — ties are silence, the battery is a conjunction. | Session decision (Micah, 2026-08-04; design batch grill 2) |
| Q-97 | The record plane is FILESYSTEM-ONLY: no /v2 routes, no app screens — personas never see it, and guarded-metric numbers never reach a person-facing surface (Q-83's class, operator hat included). Ledger line format fixed (graduation/demotion/re-graduation, mechanism = registry key, one line each — rulings open item 3 closed). Verdicts are files beside the archived life they cite (`archives/eval/<cycle>/<trial>/verdicts/`), citation validation mechanical: ref must resolve into the archived life and quote must be an exact substring; malformed twice = inconclusive = not kept. Manual demote is a CLI script (`scripts/demote.ts`, works with the server down); the operator view is `scripts/loop-status.ts` with the guarded/diagnostic split visible. | Session decision (Micah, 2026-08-04; design batch grill 2) |
| Q-96 | The frozen-path self-disclosure is ADOPTED, report-only: every loop cycle report includes a `git diff --stat` of the frozen paths (dossier seeds, scoring rubric, guarded list, invariant suite). Zero-LLM, one command; drift of the 111-precedent class becomes self-disclosed record. It reports and never blocks — consistent with Q-89's prompt-level-only freezing. The halt-on-drift variant was offered and declined as a mechanism-level freeze walking back Q-89. | Session decision (Micah, 2026-08-04; frontier batch grill) |
| Q-95 | Tripwire numbers (shapes ruled in Q-90): event floor 20 per guarded metric before any judgment (below floor = `graduated-unconfirmed`); dwell 7 days in shadow before autonomous re-graduation (false demotion stays the cheap error — fast recovery preferred over maximum damping); baseline = trailing 28 days snapshotted and FROZEN at graduation, so later habit drift never silently moves the yardstick and a degrading mechanism cannot drag its own baseline down. | Session decision (Micah, 2026-08-04; frontier batch grill) |
| Q-94 | The dossier battery is ratified: FIVE archetypes, each a full invented life story — guarded speaker, verbose rambler, terse minimalist, self-reviser, eager pleaser (the sycophancy control) — every trial runs all five as a conjunction, no averaging. Each dossier scripts 2 synchronic tensions + 1 diachronic revision in frontmatter (the wiki-truer axis's checkable substrate). FULLY SYNTHETIC by ruling: no content derives from any real vault — the archived-vault derivation question is closed as never, privacy over fidelity. Format: markdown + YAML head, `eval/dossiers/NNN-*.md`, dropped whole into the persona's system prompt. Spec: `docs/loop-dossier-spec.md`; the battery freezes on authoring. | Session decision (Micah, 2026-08-04; frontier batch grill — the Q-91 ratification session) |
| Q-93 | The instance plane: the loop spawns the REAL server, one process per instance on its own port with a fresh vault dir and per-instance token — the paired trial measures the shipping artifact, never an embedded approximation. Model casting: the instance's elicitor/clerk use the same env-configured local endpoints as the owner's instance (the mechanism is measured as shipped, model included); personas are omp-dispatched agents living from Claude-authored dossiers — different model families on the two sides blunts same-model verdict collusion. Teardown = archive: whole eval vaults move to `archives/eval/<cycle>/<trial>/`, so verdict citations stay checkable forever. Spec: `docs/loop-instance-plane-spec.md`. | Session decision (Micah, 2026-08-04; frontier batch grill) |
| Q-92 | The greenfield core surface (open/say/act/view) mounts at `/v2` on the EXISTING server — old routes untouched, SPA migration stays fog until the fleet proves the surface. Auth unchanged: loopback-only + bearer token, the instance plane minting a fresh token per spawned instance; isolation comes from vault-per-instance, not auth machinery. `view` returns SHAPED PROJECTIONS ONLY (SPA parity — headless-completeness means the interview, not the filesystem); raw vault files stay reachable by the loop harness via the filesystem, outside the API. Spec: `docs/loop-core-api-spec.md`. | Session decision (Micah, 2026-08-04; frontier batch grill) |
| Q-91 | Real-corpus eval fixtures follow the archive: fresh-start moves `data/eval-*` into `archives/` as ruled the same day, and a manifest (`data/eval-fixtures.json`) records the path; the improvement loop reads fixtures READ-ONLY through that pointer and touches nothing else under `archives/`. Real prose lives in one place under one rule. Dossier seeds for the loop's personas are preauthored (Claude, to a spec ratified in one dedicated session — the last human fingerprint on the measure) and then frozen. | Session decision (Micah, 2026-08-04; measurability grill Q6) |
| Q-90 | The boundary to the owner's instance: guarded metrics — skip rate, deferral rate, refusal rate, dormancy, discomfort-gate frequency, sitting frequency/return, sitting length vs declared Mode — are observable by everything and optimizable by nothing. A zero-LLM tripwire sweep watches them post-graduation: per-metric event floors before judgment (below floor = `graduated-unconfirmed`; Q-14's one-flip-is-noise, mechanized), one-sided test vs pre-graduation baseline with ties going to shadow, BATCH demotion of all recent graduations (recency is the only admissible attribution — causal attribution at n=1 would require a model of the person, which guarded metrics must never have), dwell before re-graduation. Demotion = `live: true → false`, nothing reverted. Sounding depth is deliberately NOT guarded — the gate is watched; depth is diagnostic, and guarding it would blind the loop to descents dying early. | Session decision (Micah, 2026-08-04; measurability grill Q5+Q7) |
| Q-89 | The improvement loop runs FULLY AUTONOMOUS: it implements, lands, and graduates without per-change human review. The owner is out-of-band — real use, the Activity Log, the graduation ledger (`data/graduation-ledger.jsonl`, append-only, instrument-plane, survives fresh start: every graduation, demotion with its firing numbers, re-graduation with evidence refs), and a manual demote that always works. Re-graduation after a tripwire is autonomous; the record, not a gate, is the accountability. Apparatus freezing (dossier seeds, scoring rubric, guarded list, invariant suite) is PROMPT-LEVEL ONLY, by explicit ruling; the 111 test-amendment precedent stands registered as the known bypass risk. | Session decision (Micah, 2026-08-04; measurability grill Q3+Q4) |
| Q-88 | The paired trial is the loop's unit of measurement: one dossier seed lives twice, under variant A and variant B; the persona renders per-subsystem verdicts — which life was better interviewed — each citing lived moments. A VERDICT CITING NO LIVED MOMENT IS MALFORMED (the Wiki's citation discipline applied to evaluation itself). Behavioral revealed-preference traces (elaboration length, restatement rate, gate choices, voluntary returns) accompany as no-regression evidence. Absolute anchored scores rejected: drift plus Q-21 laundering. The rubric asks disconfirming questions, never satisfaction questions — the registered risk is optimizing for what tireless sycophancy-prone LLM readers enjoy. | Session decision (Micah, 2026-08-04; measurability grill Q8) |
| Q-87 | Elicit is evaluated by agents that USE it: each eval agent is seeded with a dossier, creates ITS OWN vault, and lives in the app across sittings; from inside that use it measures all four axes — constitution obeyed (a mechanical GATE: an invariant violation voids the run, no partial credit), wiki-truer (the persona judges claims about itself against what it knows of itself — the person of an eval vault is the agent, and the person is the authority on their own model), archive-richer (DIAGNOSTIC only: every richness metric improves under degeneration, so it may support and never suffice), person-served (first-person reportable in the loop). No composite scalar across axes, ever — Q-21 one level up. Supersedes the mechanical marker-recovery scorer proposed first: metrics not tied to human or agentic experience miss the product. | Session decision (Micah, 2026-08-04; measurability grill Q1+revision) |
| Q-85 | The DRM instrument's constitution (settles prototypes/drm/NOTES.md): affect probe OPEN first ("how did that time feel?"), dimensional guidance only as a nudge on a thin answer — explicit dimensions train the frame; `momentary-state` earns its own Facet (full consumer sweep per the 095 pattern); enumeration collects START times only — duration-guessing is rounding theater; the gate is ALWAYS visible (Q-44 applied — always present, never triggered); rotation is USER-DECLARED only, no cooldown machinery, revisit after real use; fragments pass ORDINARY harvest review with the episode shown — the standalone-interpretable hard gate holds, affect answers that need their episode fail it and bud. The prototype's direct-Snippet assumption is overruled by canon. | Session decision (Micah, 2026-08-03; DRM notes — "recs on all six") |
| Q-84 | The second perspective enters by three doors, all admitted: (1) a `role-taking` Stance — the person writes AS their model of a named other; flagged like causal-theory (evidence of their model of the other, never of the other); (2) people as grid elements — triadic comparison over gazetteer people, a protocol def with its own presentation (the first use of CONTEXT's Protocol-presentation clause); (3) other-minds Expeditions — the errand is asking someone who knows you; the corpus receives the person's reflection, ordinary admissible capture. New invariant: role-taking Snippets never evidence claims about the named other — only claims about the person and their models of people. Ascending rungs of one ladder: imagined other, structured comparison, real other. | Session decision (Micah, 2026-08-03; 099 grill) |
| Q-83 | The lineage mirror is licensed, three-quarters narrowed. The system may quote the person's usage back to them ONLY in service of a specific claim (lineage bearing on that claim's truth — never free-floating observation of habits); the quotable set is neutral usage facts already visible on the person's own surfaces (timestamps, cadence) — skips, deferrals, refusals, and dormancy are NEVER mirror material (the never-weaponized class stays sealed; Q-77's no-comment-on-absence stands); the question obeys Q-81 — two facts presented juxtaposition-style, zero assertion, the reading is the person's to make. Shadow-first (Q-35). New QuestionProvenance member with a DrawProvenance-style evidence variant naming the lineage read (Q-18's rule). | Session decision (Micah, 2026-08-03; 098 grill) |
| Q-82 | Pattern dosing: two tiers, drawn not chosen, no rollouts. Cheap derivation patterns (sentence stems, Clean Language frames, instance tests, anniversary framings) are ordinary composed questions. Deep patterns (externalizing, miracle question, heavy scenarios) are Sounding-class — consent-gated, late-session, park-anytime; membership in the existing gate, no new machinery. Within the licensed set, the PATTERN is selected by Q-13's discipline (filter, then random among survivors, never argmax) — the operator does not choose, and no pattern can become the favorite, which retires monoculture structurally. SparkMe-style conversation-rollout utility estimation is REJECTED: the strongest deployed result in the literature is a fixed script plus competent probing; rollouts are the clever selector at maximum cost. | Session decision (Micah, 2026-08-03; 103 grill Q2) |
| Q-81 | Derived questions: operators and rearrangements, never assertions — and creative within that line. A composed question may RECOMBINE elements the person named (scenario, dilemma, instance, counterfactual), recording in its Provenance which Snippet versions supplied the elements (Q-18's name-what-you-shuffled, applied to composition), and may apply CONTENT-FREE OPERATORS (suppose, time-shift, miracle premise, Clean Language frames). It may never assert or presuppose anything about the person they have not said: no paraphrase, no motive attribution, no interpretation smuggled into a premise. Enforcement is Q-36's split made concrete: full creative freedom in generation; a mechanical guard at the boundary — a derived question decomposes into quoted spans + registered operators + recombined named elements, or it is refused. Q-12's verbatim rule is satisfied by the quoted spans, not repealed. | Session decision (Micah, 2026-08-03; 103 grill Q1 — "questions should be creative" + rec) |
| Q-80 | No exhibits. Documents-of-action do not become a citable evidence class. Self-authored documents whose content should count go through the import door and become ordinary Snippets (Q-57/Q-58); declared artifacts stay scoped to the Coach under Q-78's never-open rule. The Wiki remains a model of the narrated self, evidenced by the person's words and their declared performances — no document-pointer citations, no typed artifact refs. | Session decision (Micah, 2026-08-03; 097 grill — rejected at Q1) |
| Q-79 | Territory instruments are licensed: a stored map of a domain or a life (KTG skeleton, gazetteer, atlases) with corpus-coverage states (unprobed/touched/evidenced) is the interviewer's private checklist, not a grade — coverage is a fact about the ARCHIVE (what has been asked and evidenced there), never about the person. Amends ticket 092's hard lines in scope: "never stored, never a coverage measure" bound the Coach's ADVICE plane; storage and coverage are licensed on the SELECTION plane only. Conditions, enforced as negative pins: no surface renders a map, node, or percentage; no question or advice frames a gap as a lack (an aimed question names the TOPIC like any opener, never the absence — "you've never mentioned X" is banned); no completion number exists anywhere; map-to-corpus links are readings under Q-50's status discipline, not priors. Extends to the self: atlas-aimed questions enter at opener depth, and declining works as everywhere — recorded signal, ordinary skip mechanics, no special cooling machinery. Activity Log surface says territory-aimed; node ids stay in the JSONL (038's rule). | Session decision (Micah, 2026-08-03; 096 grill) |
| Q-78 | An artifact is a DECLARED pointer plus the person's own sentence about it. The pointer (path/URL/name) is lineage-plane record; the description harvests as an ordinary Snippet. Skill-claims cite the return-Snippets and description-Snippets — never the artifact file. The model never opens, reads, or evaluates an artifact; the Coach may mention one only by the name the person gave it. 046's detection ban generalized: the system judging your output is the looping-effect harm at its worst. | Session decision (Micah, 2026-08-02; Coach grill Q6) |
| Q-77 | Coach advice is EVENT-LICENSED and capped, never time-licensed. Licensing events are all person-caused: a quest return, an artifact registration, a sitting touching the Direction, opening the Coach page. Elapsed time licenses nothing — the Coach cannot comment on absence, because dormancy is signal it reads, never a state it names (Q-24). At most ONE unread advice note per Direction (Q-56 bound, live); a new licensing event replaces the unread note rather than stacking. Declined options are never re-offered (Q-43's discipline applied to advice). | Session decision (Micah, 2026-08-02; Coach grill Q5) |
| Q-76 | The Coach surface is one page per coached Direction, in the wiki surface's pattern: a chronological log half (quests with returns, artifacts, sittings touching the Direction) and a margin-note advice half, all agent prose visually agent-plane. The waiting surface gains one quiet line per coached Direction only when something new waits since the last visit. NO global Coach tab: an aggregate view invites cross-Direction comparison — a report card, the artifact Q-24 exists to prevent. | Session decision (Micah, 2026-08-02; Coach grill Q4) |
| Q-75 | Quest lifecycle: offered → adopted → returned → retired. A RETURN is ordinary capture — a sitting turn or unprompted entry tagged to the quest, harvested by the normal path, so returns are Snippets with quest provenance; a return licenses the reflection follow-ups (what broke, what surprised). Retired is the person's verb; no failure state, no expiry, and no completion rate computed or shown anywhere — a rate is a streak wearing statistics. | Session decision (Micah, 2026-08-02; Coach grill Q3) |
| Q-74 | A doing-quest is OFFERED AS A SET of 2-3 alternative concrete acts, each citing the claims that make it relevant; the person adopts one, several, or none. Adoption mints the quest record; unadopted alternatives evaporate without residue. No deadlines, no reminders (Q-22); a dormant adopted quest waits as signal. Quest text is agent prose — Marginalia-class, never quotable into a Piece; the person's return is the harvestable part. A single proposed next step is a prescription wearing a quest costume (Q-24). | Session decision (Micah, 2026-08-02; Coach grill Q2) |
| Q-73 | A Direction becomes COACHED only by the person's declaration; the agent's only move is a Q-37-shape dimmed offer when a Direction accumulates skill-claims (live per Q-62 — declining costs a word). Coached is reversible; un-coaching stops the Coach lens and archives nothing. Agent-selected coaching would be an act (Q-35 shadow-first → inert), and auto-coaching every work Direction spreads the Coach into ambient noise. | Session decision (Micah, 2026-08-02; Coach grill Q1) |
| Q-72 | Dangling-referent repairs are Buds through the ordinary Queue, capped — no repair surface exists. One Bud per unresolvable dangler minted at commit time (measured base rate: 25/139, 18%); repairs surface one at a time inside ordinary sittings via the Queue's existing caps. A screen listing outstanding repairs is debt rendered as a list, which Q-24 forbids; Q-6 already says a Bud waits without accusing anyone. | Session decision (Micah, 2026-08-02; Seeding grill Q7) |
| Q-71 | A retelling Link is NO NEW OBJECT. Retellings are cross-sitting pairs — exactly what the Q-65 ranked channel surfaces first and the contradiction plane records as the diachronic type (tension is the finding; no resolution sought). A second linking store would need its own lint, surface and staleness rules to restate what the Contradiction plane already says. Seeding adds only the negative rule: idempotent ingest dedupes on exact text WITHIN one source path (Q-59 identity), never across paths — the same sentence in two files is precisely the retelling to keep twice. | Session decision (Micah, 2026-08-02; Seeding grill Q6) |
| Q-70 | Authorship is declared PER REGION at Reach time, reusing 046's field with three values: `authored \| other \| machine-assisted`. Detection stays banned (046, permanent). Non-authored regions still harvest — kept words are evidence of the keeping — but their snippets never carry `stance: avowal`; the reading describes what was kept. Mixed folders: move files, not flags — the person's organisation is the declaration instrument. Per-item declaration is the attention cost Q-60 refused. | Session decision (Micah, 2026-08-02; Seeding grill Q5) |
| Q-69 | Reach is OFFER-ONLY: when a live Direction touches an unharvested region, the agent may show a dimmed line naming it on the waiting surface (Q-37's shape) — never pull the region on its own judgment. Ships LIVE under Q-62: declining costs a word, silence costs nothing. Extraction on agent judgment would be an act → shadow-first → inert, the failure class this project has hit six times. | Session decision (Micah, 2026-08-02; Seeding grill Q4) |
| Q-68 | Q-58's no-batch-accept survives scale UNTOUCHED; scale is handled by bounding input, never by weakening review. A region is a FOLDER SUBTREE — the person's own organisation, model-free, with the Survey map's harvested/unharvested state computable per node. Reach hands over one region at a time and extraction runs under the live caps (Q-56), so the review queue is never longer than the region the person chose. A faster accept is not a smaller gate but no gate: misleading excision is invisible without reading. | Session decision (Micah, 2026-08-02; Seeding grill Q3) |
| Q-67 | Q-57 amended for undated corpora: a region may declare ONE mechanical dating rule at Reach time — a frontmatter key OR a filename pattern (`2021-03-04.md`) — and every file matching it is dated by that declaration; every non-matching file is refused BY NAME. What Q-57 bans is the guess (mtime above all, a lie for anything ever copied); a date typed into a filename is a declaration made at the time, the same epistemic class as frontmatter. Per-item dating questions stay banned at scale: they ask 2026 to testify about 2019. | Session decision (Micah, 2026-08-02; Seeding grill Q2) |
| Q-66 | The "weak prior" does not exist; Q-50 stands unmodified over seeded material. Two dated pieces years apart are two sittings, and a claim citing both promotes to `evidenced` mechanically — sittings years apart satisfy "survived being approached again on a different day" better than most live pairs. Confirm becomes a LICENCE, not a status: a seeded claim licenses a Still-true revisit (asked differently, Q-14) and never waits in a discounted state. Every rival (fifth status, cite discount) reintroduces confidence numbers under a new name, which Q-21 forbids. | Session decision (Micah, 2026-08-02; Seeding grill Q1) |
| Q-65 | Same-sitting pairs RANK BELOW cross-sitting pairs in the contradiction candidate pool — pooled, never excluded. Cross-sitting drift fills the judgment quota first; within-document incoherence is judged only when room remains. Grounded in ticket 007's measurement: the corpus has a hard ceiling at 0.640 for cross-sitting cosine while intra-document pairs run to 0.808, so any workable threshold alone yields an all-same-essay pool — the channel measuring how tightly an essay stays on topic instead of how a belief moved across years. Q-50's logic one layer up says same-sitting opposition is usually rhetoric (concession, setup); a measured long-form counter-example says it is sometimes real incoherence — ranking below serves both where exclusion would blind the channel to the second. Implementation rides ticket 083 with the rank-contract change. | Session decision (Micah, 2026-08-02; grill of ticket 064) |
| Q-86 | Agent-plane summaries may INFORM composition but are never QUOTABLE material; quotes come from Snippets alone; the boundary is a code guard (`checkSummaryEcho` in `src/clerk/composed.ts`), not a prompt instruction. | Session decision (119 grill) |
| Q-62 | **Q-35 amended a second time, and the exception count returns to zero: a mechanism whose only power is to OFFER — one proposal the person declines in a word, with nothing done on decline — ships LIVE from day one, logging every evaluation it makes; a mechanism that ACTS on its own judgment stays shadow-first exactly as Q-35 says.** The dividing line is the consequence on silence: if the person ignoring the mechanism means nothing happens, it is an offer; if ignoring it means something happened anyway, it acts. This retroactively re-grounds Q-49 (the opposition gate is an offer-shaped bound) so Q-35 no longer carries a named exception, per Q-56's own form: amend the principle, never accumulate footnotes. First application: the Soundings entry license (Q-43) ships live — shadowing it would mean no Sounding is ever offered and the slice lands as dead code, the exact inert-mechanism failure this project has now hit six times. | Session decision (Micah, 2026-08-02; Soundings plan Blocking Open Question 1) |
| Q-63 | Q-47's late-entry reading: when a Sounding is licensed with fewer than 8 questions of budget remaining, the rung allowance FLOORS at 8 and the sitting grows past its declared minutes. "A Sounding becomes the rest of the sitting" is taken literally — the descent is the sitting from that point, the two close moves stay reserved beyond the allowance, and the consent ask states the real expected length, which is what keeps the overrun consented rather than suffered. The rival reading (license requires ≥8 remaining) was declined: it makes late-sitting offers rare in exactly the short Modes where a held thread is most likely to be worth descending. | Session decision (Micah, 2026-08-02; Soundings plan Blocking Open Question 2) |
| Q-64 | The gate's third word, "another day", writes the full ladder to the vault and mints NO Queue pointer: the record survives (Q-45's preservation is untouched), but the thread does not resurface unless the person goes looking. This is what distinguishes it from "park, depth kept", which mints the resumable pointer. Three words, three distinct outcomes — continue descends, park suspends with a thread back, another day closes the door without burning the record. If both of the last two minted pointers, they would differ only in horizon and the gate would really be two words wearing three labels. | Session decision (Micah, 2026-08-02; Soundings plan Blocking Open Question 3) |
| Q-1 | Sole Authorship: the agent never writes Snippet or Piece prose. Harvest cuts are exact substrings of the user's text, enforced in code, not by prompt. | CONTEXT.md — Sole Authorship, Harvesting |
| Q-2 | Local models only. All inference targets a local OpenAI-compatible endpoint. No hosted API calls, ever. | ADR-0001 |
| Q-3 | Markdown files are the source of truth. Any index is derived and rebuildable from the files alone. | Session decision (Q11), OptMem review |
| Q-4 | Facet and Stance are agent-authored Wiki readings that cite `snippet@version` — never fields inside the Snippet file. Snippet files hold only user prose plus Provenance. | CONTEXT.md — Wiki, Snippet |
| Q-5 | Snippet versions are immutable. An edit creates `v(N+1)`; old versions remain. | CONTEXT.md — Snippet |
| Q-6 | Fragments failing admissibility become Buds with their failures recorded — never edited into shape, never discarded silently. | CONTEXT.md — Bud |
| Q-7 | Mode constrains what is askable; it is never the optimization objective. Facet distribution is tracked per Mode. | CONTEXT.md — Mode; research §2-3 of contradictions |
| Q-8 | Transcripts are append-only and never edited. Agent summaries of Transcripts are Marginalia-class, barred from Pieces. | CONTEXT.md — Transcript |
| Q-9 | Interface draws from focus-friendly markdown editors: one column, no chrome, focus dimming, typographic hierarchy, no chat bubbles. | docs/interface-references.md |
| Q-10 | LLM access goes through `@mariozechner/pi-ai` (unified provider layer); pi-coding-agent machinery adopted only if needed. | Session decision (pi verified on npm, v0.73.1) |
| Q-11 | Fluency, vividness, specificity, and confidence are forbidden as Snippet quality signals. Only structural signals count. | research-shape-of-the-problem.md §(a) |
| Q-12 | A composed question must contain the user's quoted fragment as an exact substring — the question-side mirror of Q-1, enforced by the same mechanism. | research-question-policy.md §(a) |
| Q-13 | Question selection is constraints-then-chance: hard filters (license, Mode, Facet balance, weak-early ordering, exposure), then top-k uniform random. Never argmax; never scored by fluency or plausibility. | research-question-policy.md §(c)(d) |
| Q-14 | One flipped answer never opens a synchronic Contradiction — re-measure the construct a different way first. Still-true checks always ask differently. | research-question-policy.md §(d)8 |
| Q-15 | Verification questions are a last resort: show the claim in the open Wiki and let the user refute (Propagation). Confirm-everything is the known-worst policy. | research-question-policy.md §(a)7 |
| Q-16 | The Randomizer's draw is never vetoed by the agent, and it is licensed by dry spells and stale regions — defended on coverage grounds, not creativity. Session budget: 10–20 questions. | research-question-policy.md §(a)5, (c) |
| Q-17 | Resonance retrieval is a staged hybrid: lexical (phrase-echo, explainable, quotable) ships first; a local-embedding channel for disjoint-vocabulary clashes lands with the Clerk slice. The index stays derived and rebuildable. Embedding endpoint verified live: Ollama at the local `/v1/embeddings` endpoint — `qwen3-embedding` (4096-dim, provisional default; multilingual) and `nomic-embed-text` (768-dim, lighter). | Session decision (grill Q16); idiolect argument + Q-12 fit |
| Q-61 | The vault is a git repository, and the docket commits it — one commit per run, message from the `DocketReport`, authored as `elicit-clerk <clerk@localhost>`. This is HISTORY, not backup: it does not answer the standing no-offsite-copy risk (ticket 017, declined and still declined). Done NOW rather than later, because initialising an empty vault costs one command while initialising it after a bulk import makes the first commit a wall of already-written files — a witness that saw nothing. Not a manual habit: a habit produces a repo with four commits and nothing after, which is worse than no repo because it LOOKS tamper-evident. The distinct author is where tamper-evidence actually pays — a hand edit and an app write become distinguishable in `git log` without inspecting content, which is otherwise unrecoverable. A dirty tree at boot is COMMITTED, never refused: a dirty tree means a crash mid-write or a hand edit, both of which the history should record, and refusing to boot would make git a gate on the product working — the vault is the thing, git is the witness, and a witness never stops what it witnesses. Ignored inside the vault: the derived index and the vector cache (Q-3, rebuildable, and a 4096-dim vector per claim would dominate every diff with noise) and `.auth.json`, which is a credential rather than corpus and would put the Q-25 password lock into an object store that outlives deletion. `log/` stays IN — it is Q-23's ledger, not derived. **The property change, stated so it was decided rather than discovered: a repo makes deletion hard.** Plain files meant `rm` was final; now anything removed persists in the object store until someone knows enough git to rewrite history. Acceptable because the design already refuses deletion everywhere (Q-33 archives with a reason; the claim store has no `unlink`), but it is a real change to what "I want this gone" costs. | Session decision (Micah, 2026-08-02); HANDOFF review finding |
| Q-60 | Imported items carry NO Target, and no control is offered to set one. Q-55 makes this decisive rather than merely defensible: Target is a hard filter that is never relaxed at any rung of the degradation ladder, so a wrongly-assigned Target permanently and silently removes that material from half of all sittings, while an absent Target serves both (045). Asking once per batch guarantees a wrong answer, because a folder is heterogeneous — a years-spanning corpus can hold work case studies and personal essays together, and one label mislabels roughly half. There is no batch-level answer that is true. A per-piece control at review time is also declined: it competes with the three verbs (Q-58) for attention, and the failure it would prevent has not been observed. If openers minted from imported material start landing in the wrong sittings, that is a ticket with evidence behind it — the same discipline as Q-52. | Session decision (Micah, 2026-08-02) |
| Q-59 | An imported file's identity is its CONTENT HASH, and a changed file is a NEW ITEM rather than a new version. Identical hash on re-import → skip, which is what "re-running imports nothing twice" means. A changed file imports as a SECOND dated sitting, dated to frontmatter `lastmod` where present and refused under Q-57 where absent; the original sitting stays untouched forever. Q-5 snippet versioning does NOT apply: versioning is for text the user revises inside Elicit, and a file edited on disk is not a revision of a snippet but a new document sharing a title. Cuts already taken are not re-proposed — dedupe on exact snippet text within the same source path, so an edited file offers only what is new. Rationale: if a changed file became a new VERSION of the original snippets, the wiki would claim prose written in 2027 was written in 2018, which corrupts Q-50 cite independence at the root — the date is the only thing that makes an imported sitting independent evidence — and is the same diachronic corruption Q-34 refuses to inject on model upgrade, arriving through a different door. The property this preserves is more than bookkeeping: an edited old post becomes its OWN evidence of drift, two independently dated sittings years apart on the same material, which is the highest-value pattern the system exists to find and which the versioning answer would have destroyed. | Session decision (Micah, 2026-08-02) |
| Q-58 | The import review IS the harvest review (`web/main.ts:975`), pointed at an imported piece instead of a sitting transcript — same surface, same controls at the point of attention, minus `restate`. Three verbs: approve / trim / discard. Restate is dropped because you cannot restate a 2018 essay: you are not the person who wrote it, and a restatement would produce prose from today wearing an eight-year-old date. ONE CHANGE, forced by the source: the piece renders WHOLE, in order, with proposed cuts marked IN PLACE, rather than as the flat list `renderProposal` builds today. For a sitting a flat list is fine — you said the words eleven minutes ago and still hold the context. For prose from 2018 you do not, and context is the only thing this review can catch: fabrication is already caught structurally by the substring check (the 2026-08-02 dry run dropped one such piece with no human involved), so what survives and still deceives is MISLEADING EXCISION — a sentence exactly yours that means something else lifted out — and judging that requires seeing what sat on either side. PER-ITEM AND RESUMABLE: each piece commits as its own dated sitting and the next waits, so the waiting-state vocabulary already speaks it, the work splits across weeks, and Q-50 independence holds regardless of review order. NO BATCH ACCEPT: a control that skips reading converts the gate into theatre, because the one failure mode this surface exists to catch is invisible without reading. Extraction runs AHEAD of review in the docket under 047's single-flight, so the ~40s/chunk is paid before the person sits down and the browser may close. Framing: nineteen pieces is not a chore surface but the FIRST re-reading session (Q-27), arriving before the wiki exists rather than after — which is the right way round, because it is the only chance to refuse a sentence before it becomes a Claim about you. | Session decision (Micah, 2026-08-02) |
| Q-57 | The importer has ONE DOOR: a folder of files on disk. The app never opens a socket, and the date comes from frontmatter in the file or the file is not imported. ADR-0001 is not the reason — it governs what LEAVES, and a fetch receives — but three others are. (1) A folder is the only door that already fits every source: a blog export, a photo-feed export, an Obsidian vault, a Hugo `content/posts`, a folder of `.txt`. Building it builds all of them; building a feed fetcher builds one. (2) **Fetching destroys separability, which Q-51 makes non-negotiable.** A feed hands over rendered HTML; the quotations that nearly entered the measured corpus were caught only because the markdown SOURCE preserved inline citation syntax, and one of them had neither quote marks nor its own citation and was catchable purely because the adjacent paragraph survived as a paragraph. Rendered output flattens the structure the catch depends on, so a fetcher would import someone else's sentence as the user's and be structurally incapable of noticing. (3) It puts the corpus boundary back where Q-51 says it belongs — deciding which talks were co-authored, which posts are captions, which drafts were never yours is the judgement Q-51 rules cannot be automated, and assembling a folder makes it explicit, manual and once. Consequence: each hosted source becomes an EXPORT SCRIPT that writes a folder, never an importer, so each source's authorship question is answered by a human writing that script. Dates: frontmatter only — never mtime (a lie for anything ever copied), never inferred, never asked per item. A file with no date is refused with a reason rather than guessed, because under Q-50 the date is what makes cite independence possible at all, and a guessed date silently corrupts the single property that makes import worth doing. | Session decision (Micah, 2026-08-02) |
| Q-56 | Q-35 governs SELECTION mechanisms; BOUNDS ship live from day one. The distinction is in what not-acting means. A shadowed selection mechanism draws from the unnarrowed pool — a coherent, safe default, and the mechanism's absence is the status quo. A shadowed BOUND is not a bound at all: a cap of 2 in shadow writes "I would have stopped at 2" while the system mints without limit. That is worse than having no mechanism, because the mechanism is what generated the work in the first place — on the imported corpus, a shadowed `remeasure.liveCap` means the Clerk's first docket run fills the Queue with nothing but re-measures and the person opens their next sitting to a wall of "you said X, is that still right?". So caps, quotas and rate limits (`remeasure.liveCap` 2, the sweep call quota 12, the opposition judgment quota 3, and any future one) are LIVE at birth. What they still owe is the record: every clip emits `threshold-clipped`, so a PROVISIONAL value stays honest and re-tuning it after RESULTS is one line with evidence behind it. Stated as an amendment to Q-35 rather than as a per-threshold exception, because a principle that accumulates footnotes dies without ever being overruled. | Session decision (Micah, 2026-08-02) |
| Q-55 | The degradation ladder is TWO RUNGS AND A COMPOSING FLOOR, and the principle behind it is that the system drops its own inferences before it drops the person's declarations, and when it runs out of inferences to drop it composes rather than compromises. Rung 1: drop facet balance (already shadow; the system's inference about corpus shape, and it already cannot empty the pool because `draw()` returns null before it runs). Rung 2: re-admit entries whose `source` is `user-declared` that sharpness or modeNeeds excluded — the Q-20 bookmark was asked for by name, and the system's judgement that it is too sharp for an opening is exactly the judgement that should yield to an explicit request. Nothing else is re-admitted. NEVER RELAXED: `status` (asking an answered question is incoherent, not a trade-off), `Target` (Q-19/045 — one composed self entry hijacks a declared domain sitting; the person said what this sitting is FOR), and `modeNeeds`/`horizon` for agent-minted entries. Rationale, and it inverts the framing the question arrived with: the fall-through is not a failure state. An empty draw makes the elicitor compose fresh with full context under Q-36, which is usually BETTER than a banked agent-minted candidate from weeks ago that only fits because a filter was relaxed. So the ladder is not choosing between a good outcome and a degraded one but between stale-but-banked and fresh-but-unbanked, and fresh usually wins — which makes a long cascade actively harmful. Each rung emits one `queue-rung` event naming the relaxed constraint and the pool size before and after, so "five filters emptied the pool" becomes measurable rather than assumed. | Session decision (Micah, 2026-08-02); HANDOFF review finding |
| Q-54 | Context-dependence is a RANGE REFINEMENT, not a third Contradiction type, and Q-21 already built the vocabulary for it: Range is mandatory on every Claim precisely so a belief cannot be stated context-free. Two claims whose bodies oppose but whose Ranges do not are not in contradiction, they are correctly scoped; the un-drawn boundary is the case where both Ranges are the same or both vague. So the answer to "where does the first hold, where does the second" is a narrowed Range on EACH pole — `SUPERSEDE` with reason, a verb Q-29 already has. No third Contradiction type, no new op, no new Protocol instrument. Mechanism: a fifth `ClashCandidate` outcome `range-discriminated`, whose consequence is one SUPERSEDE per pole with reason `range-discriminated:<candidateId>`, citing both poles' snippets plus the discriminating answer's. Because Q-53 forces that answer into a different sitting from both poles, Q-50 makes the refined claims born `evidenced` rather than `unconfirmed` — the most valuable claims in the wiki arrive with the strongest status mechanically, without anyone deciding it should be so. The discriminating question comes AFTER the re-measure and only on the dissolution branch, never instead of it: asking "when does X hold versus Y" before opposition is established presupposes both poles and leads the witness, and Q-30 stage 2's "exactly one question" is a rule about the gate on opening a Contradiction, which a follow-up on an already-dissolved candidate is not. Often no second question is needed at all, because the dissolving answer named the condition itself. TWO DOORS, and the second ships first: the pipeline door above inherits every recall problem in Q-52, and hanging the highest-value output in the system off its flakiest machinery is not acceptable — so a zero-LLM lint finding under Q-31 (two live claims sharing a registry referent whose Ranges are identical or near-identical) mints discriminating questions independently, with no candidate pool and no opposition detection in the path. It works on the corpus that exists today. | Session decision (Micah, 2026-08-02); HANDOFF review finding |
| Q-53 | A re-measure counts only from a DIFFERENT SITTING, and the predicate is the session boundary rather than a wall clock. The confirming reading's `Provenance.session` must differ from the session of BOTH claims in the pair — not merely be later than `remeasureAskedAt`, which only records when the question was minted. Computed mechanically from the same field and the same cite→session resolution Q-50 uses; one rule, two uses. Rationale: the failure Q-14 exists to prevent is LABILITY UNDER QUESTIONING, and lability is a property of a continuous conversational frame — the momentum of the last half hour, the vocabulary the elicitor introduced, the self the person has been performing. A session boundary ends that frame; elapsed time does not track it. A 24-hour minimum is wrong in both directions: two short sittings twenty minutes apart on unrelated topics are separate measurements, and one six-hour sitting is not. Session is not a proxy for time here, it is the variable. Corollary on expiry: `remeasure-expired` is the ONE outcome that does not retire the pair, because expiry is a question that fell off the queue, not an answer — silence must never stand in for a verdict. A candidate carries an `attempts` counter, earns exactly one re-proposal after an expiry, and retires on the second; every other outcome retires the pair at once (B9 anti-repetition otherwise intact). | Session decision (Micah, 2026-08-02); HANDOFF review finding |
| Q-52 | The clash channels retrieve ABOUTNESS; polarity is judged one layer down and nowhere else. Lexical (shared phrase), referent (shared registry entity) and embedding (cosine) all answer "are these two claims about the same thing" — none can see negation, and none is asked to. `judgeOpposition` is the polarity organ, and it is anchored: `poleA`/`poleB` must be exact substrings of the cited quotes, code-verified, or the candidate drops. A channel that COULD see negation would separate the two poles of a contradiction and never pool them — negation-blindness is what makes an opposed pair a near-neighbour, so the property the 2026-08-02 HANDOFF review read as the blind spot is the retrieval behaviour the pipeline depends on. What the review is right about is not recall but PRECISION: an aboutness-only pool spends a bounded judgment quota on pairs that mostly agree, and `WikiReport` today cannot tell "the pool was empty" from "the pool was full of agreement" from "no contradictions exist" — the observability failure of eval finding #8 repeating. So no fourth NLI channel is added on argument: the pool is instrumented first (size, per-channel contribution, opposed ratio), and a cross-encoder graduates on that record under Q-35 or not at all. Adding a mechanism to fix an unobserved failure is the thing Q-35 exists to prevent. | Session decision (Micah, 2026-08-02); HANDOFF review finding |
| Q-51 | Material whose authorship cannot be SEPARATED is not admissible corpus, and the test is structural rather than a judgement about quality. Co-authored prose — a jointly-run talk, a co-written piece, notes attributed to two people — is excluded whole, because no cut of it can be verified as the user's own sentence and Sole Authorship (Q-1) guarantees wording rather than origin. Ruled 2026-08-02 over a published-writing ingest: co-authored material stays out. The rule extends past that ingest — anything entering the vault by a path other than elicitation must be separable, and where it is not, the whole item is excluded rather than sampled carefully. Quoted and cited passages inside a single-authored work are the same problem at a smaller scale and are excluded at cut level, not item level. Exclusion is reversible and admission is not: a wrongly-excluded item can be ingested later, while a wrongly-admitted sentence becomes a Claim about a person who never held it. | Session decision (Micah, 2026-08-02) |
| Q-50 | Two cites are INDEPENDENT only when they come from different SITTINGS. Cross-sitting is the floor and promotes `unconfirmed` → `evidenced`; cites additionally separated by Facet or by question source are the stronger tier and are recorded as such, so a later decision can use the distinction without re-deriving it. Rationale: two cuts from one answer on one thread are one thought said twice, and if that promotes a claim then every claim reaches `evidenced` within a month and the Status vocabulary stops carrying information — the specific way epistemic metadata usually dies. What `evidenced` is meant to mean is that a claim survived being approached again on a different day. The predicate is mechanical and computed from Provenance.session, never model-judged (Q-29). Must be in force before the Clerk's first run: `evidenced` cannot be retrofitted across a claim graph. | Session decision (Micah, 2026-08-02); HANDOFF review finding |
| Q-49 | The contradiction pipeline's stage-1 opposition judgment ships LIVE, not shadow — the one deliberate exception to Q-35's shadow-first rule, decided with the argument recorded. Q-30 defines a candidate AS two claims with model-judged opposed Stances: the judgment is what makes a candidate exist, not a gate applied to one, so shadowing it means no candidate ever exists and Q-30 stages 2-5 are dead code. What Q-35 protects against is a mechanism ACTING on an unproven judgment; here the only consequence is one guarded, non-accusatory re-measure question the user may ignore, and the irreversible step — opening a Contradiction — stays gated by stage 3's code checks over a verbatim quote (Q-44/Q-46 satisfied). Every other threshold in the slice remains shadow-first. | Session decision (Micah, 2026-08-02) |
| Q-48 | Two models by ROLE, not one model for everything. The ELICITOR (probes, red-lights, live composition — a human is waiting) uses the fast model; the CLERK (harvest, wiki jobs, consolidation, minting — background work, Q-22 means nobody waits) uses the careful model. Measured basis: bonsai-27b answers in 3-9s but collapsed on long structured payloads and produced echo-degenerate probes; qwen3.6:35b returns clean JSON first try but is far slower per call. Latency is a UX constraint only in the foreground; instruction-following is a correctness constraint only in the background. Both stay local (ADR-0001). Q-34 model stamps must record WHICH model produced each artifact, so the two roles remain separable in the record. Either role can be pointed anywhere by env var; the split is a default, not a coupling. | Session decision (Micah, 2026-08-02) |
| Q-47 | A Sounding BECOMES the rest of the sitting: entering converts the remaining question budget into the descent's rung allowance (capped 8-12 per CONTEXT), and the two close moves (Q-20) are reserved BEYOND it, always. Rungs neither silently consume the close nor run unbounded — an uncapped consent-gated descent is how consent becomes an endurance test, however good the gates are. Because the total is knowable in advance, the user is told roughly how long the descent runs when asked to consent (Q-43), which is what makes the consent meaningful. A sitting that went deep is the one whose bookmark matters most, so the close is never sacrificed. | Session decision (Soundings grill Q5) |
| Q-46 | A Sounding ends structurally, never by model self-report: at the rung cap (8-12, mechanical), OR at convergence detected in CODE — the last two rungs produce answers that echo prior rungs (the same lexical machinery Resonance uses) or an answer is content-free by the pivot heuristic already in the elicitor. The agent is never asked "have we converged?" Same principle as Q-44 and adversarial-eval finding #6: no model self-report where a structural check will do. Ending is always announced as the descent closing, never as the user failing to go deeper. | Session decision (Soundings grill Q4) |
| Q-45 | A parked Sounding preserves THE LADDER: the ordered rungs (each question with the answer it drew), where the descent stopped, and which gate choice ended it — not a bare rung number (useless without the chain) and not a pre-composed next question (authored for a person who no longer exists by the time they return; compose fresh at resume, when what happened since is also visible). RESUMING IS COMPACTED, because the local model degrades on long payloads (adversarial eval finding #1): the prompt carries the last 1-2 rungs verbatim plus a one-line summary of the earlier ones, reusing the Cover consolidation mechanism (ADR-0002 layer 3). The full ladder stays on disk as truth (Q-3); the compaction is a prompt-time view. Ladder summaries are agent prose — Marginalia-class, never a Snippet, never shown at close (Q-20). | Session decision (Soundings grill Q3) |
| Q-44 | The mid-descent consent gate is ALWAYS PRESENT, never triggered: every rung carries the same three quiet margin words (continue / park, depth kept / another day), plus a mechanical checkpoint at the halfway rung. Discomfort is NEVER model-inferred — a model self-reporting a boolean is a suggestion, not a check (adversarial eval finding #6), and applying that to a person's emotional state is presumptuous as well as unreliable: an agent that says "I sense this is hard" on pattern-matched short sentences has told the user something false about themselves, the looping-effect harm the design guards everywhere else. An always-available gate is stronger than detection because stopping never requires being noticed; the counter-based checkpoint breaks answering-momentum without diagnosing anyone. Recorded signal is behavioral only — where you parked, how long you took, whether you chose another day. | Session decision (Soundings grill Q2) |
| Q-43 | A Sounding is agent-proposed and user-consented, never auto-entered. The PROPOSAL itself is licensed: late in the sitting, a Mode with energy for it, and a thread that has already sustained 3+ turns on one construct. Declining costs one word, is recorded as signal, and is never re-offered in the same sitting — a second ask converts an offer into pressure. Consent to ENTER is not consent to CONTINUE (the mid-descent gate is separate). Rationale: auto-entry is the interrogation Q-15 forbids; user-initiation alone would require the person mid-thought to self-diagnose that a thread has become load-bearing, which is the burden the tool exists to carry. | Session decision (Soundings grill Q1) |
| Q-42 | The composition slice ships in two passes, both in scope, in this order. Pass 1 is ZERO-LLM and complete on its own: manual initiation, one deterministic chronological Arrangement, reorder / remove / write-new-prose (Q-40) / insert-Gap (Q-39), export to markdown with pinned versions inlined, manual set-down (Q-41). Pass 2 adds the model: candidate Arrangements under distinct organizing principles (Q-38), skeleton Marginalia, stale-pin lint, auto-set-down. Agent OFFERS (Q-37) are excluded from both — they are licensed by citation-cluster density, which requires Claims, so they land with or after the Clerk slice. Rationale: arranging your own sentences is the product thesis and needs no model; a deterministic reviewer cannot hallucinate, cannot stall on a slow call, and makes pass 2 a pure addition rather than a correction. | Session decision (composition grill Q6) |
| Q-41 | A Piece is never "finished" — it is SET DOWN, reversibly. Setting down (by the user, or silently after long dormancy) stops Gap-question minting and lets existing gap questions expire on the normal queue rule; the Arrangement stays fully editable and picking it up resumes minting. No done flag, because "done" manufactures its complement — everything else becomes unfinished, which is the shame gradient Q-24 forbids (dormancy is signal, never debt). A set-down Piece stays first-class: its pins still count as citations, its Snippets still resonate, it still appears in re-reading. Auto-set-down is silent and logged (Q-23), never announced (Q-22). | Session decision (composition grill Q5) |
| Q-40 | Prose the user writes inside a Piece (transitions, connecting clauses, titles) becomes a Snippet — harvested like any other user prose, with composition provenance. One rule holds with no exceptions: all user prose in the system is a Snippet, and a Piece contains only Snippets. No second class of words that look like evidence but are uncitable and unversioned; the reviewer is another elicitation surface, and writing pulls corpus the same way it pulls questions. A title is body text, so a title is a Snippet too. | Session decision (composition grill Q4) |
| Q-39 | Gaps and stale pins are annotate-and-offer, never act. Gap detection is dual-authority (model marks empty slots while arranging; the user inserts them during review — same object, user-inserted gaps mint user-declared-weight questions). Each gap mints exactly one Gap-fill Queue question and clears only when the USER places a snippet; a fitting later harvest may be offered in the margin, never auto-placed. Stale pins (pinned snippet@vN where vN < latest) get a dimmed flag that NEVER auto-updates the pin — pinning a past self's words can be deliberate, and keeping an old pin against a newer version is itself diachronic signal; the choice is recorded either way. | Session decision (composition grill Q3) |
| Q-38 | Arrangement candidates: at most 3, usually 2, each under a DIFFERENT named organizing principle (chronology / argument / contrast) — never shuffles of one principle. Skeleton Marginalia names the principle and each snippet's role. Generated at acceptance time only — no warm speculative Arrangements in the Docket. Candidates contain snippet-version pins and Marginalia only: no agent transitions, no titles (a title is body text — Q-1). | Session decision (composition grill Q2) |
| Q-37 | Piece proposal licensing: user-initiated always; agent-side offers are PASSIVE — a dimmed lint-note-style line on the waiting surface, licensed by citation-cluster density (snippets interlinked through shared claims/contradictions, never topic-count alone — a pile is not a piece), threshold shadow-first (Q-35). Ignoring an offer is recorded signal, never escalated (Q-22). Writing pulls elicitation; the agent's only edge is knowing where the material got dense. | Session decision (composition grill Q1) |
| Q-36 | Question generation follows one standing principle: freedom in generation, rigidity in validation. The model receives ever more CONTEXT (transcript, resonance, and eventually the wiki, facet balance, exposure history, Mode) and ever fewer INSTRUCTIONS (repertoire, never prescription; quoting available, never mandatory for live probes — Q-12's verbatim rule binds composed questions only); every invariant is enforced by code guards at the boundary (parrot, degenerate, no-repeat, no conversation-reference, pivot-on-thin). Born from live-sitting evidence: prescriptive prompts produced quote-frame monoculture and echoes; see docs/question-composition.md. | Session decision (user diagnosis, ticket 031 + eval evidence) |
| Q-35 | Calibration is shadow-first graduation, per mechanism, no global calendar: every selection mechanism (facet-balance filter, exposure control, FSRS horizons, uptake signals — and any future one) runs in log-only shadow from day one, writing what it WOULD have decided to the Activity Log (Q-23, free evidence); each graduates to live individually when its shadow record over enough data earns it; every PROVISIONAL register entry names its graduation condition, not a date. Calibration is not a period the product passes through but the permanent rule for how mechanisms earn the right to act. **Scope narrowed by Q-56: this governs SELECTION mechanisms; bounds ship live. Narrowed again by Q-62: offer-only mechanisms ship live with logged evaluations; only mechanisms that ACT stay shadow-first.** | Session decision (Clerk grill Q8) |
| Q-34 | Model lifecycle: every agent-authored artifact (reading, claim, composed question, candidate judgment) carries a model stamp at creation. On upgrade, re-annotation is LAZY — the new model re-reads only what the Docket touches anyway; disagreement SUPERSEDEs the old reading with reason model-upgrade (Q-29 machinery, kept never deleted). Full re-annotation batch exists but only the user triggers it. Rationale: the wiki's history records drift in the person; a mass rewrite on upgrade day injects a discontinuity indistinguishable from self-change (Q-27) — lazy keeps model-drift diffuse, and stamps make it subtractable. | Session decision (Clerk grill Q7) |
| Q-33 | The wiki-editing surface is six verbs on a claim: Attest (→ user-attested), Refute (→ contested + Q-14 re-measure, or refute-with-prejudice → ARCHIVE with the user's reason — evidence, never deletion), Edit Range, Detach cite (status recomputes mechanically), Propagate (Q-4), Edit body — which flips the claim to user-attested automatically: the moment the words are the user's, the epistemic class changes. Agent prose and user prose never blur. | Session decision (Clerk grill Q6) |
| Q-32 | Identity registry write tiers: the model MINTs new referents freely (reversible), proposes-and-applies aliases (reversible; a wrong alias surfaces as an absurd Juxtaposition and is edited away), and NEVER merges canonical identities — merge candidates surface as dimmed notes on both registry entries (Q-31 mechanism) and only user attestation executes the merge. Principle: the model may add structure, link reversibly, never collapse; who counts as "the same" is self-description, not inference (Hacking). Registry entries are vault markdown (Q-3). | Session decision (Clerk grill Q5) |
| Q-31 | Staleness lint is graph computation, zero LLM calls, with one mechanical consequence per finding: stale citation (new snippet version flags citing Claims) → one still-true question minted per flagged Claim via the composeStillTrue path, tagged to clear on harvest; orphan claim (all cites superseded/archived) → dimmed lint note in the wiki, never auto-archived; god-node Facet → note only, no split op. Underlying rule: lint may ADD questions and annotations; it may never remove or restructure claims — the zero-LLM layer is incapable of damage by construction. | Session decision (Clerk grill Q4) |
| Q-30 | Synchronic contradiction pipeline: (1) candidates from both channels (lexical + embedding) into one Clerk-internal pool — two Claims on one construct (alias registry) with model-judged opposed Stances, both poles quoted; never user-facing. (2) Each candidate mints exactly ONE ask-differently re-measure question (Q-14, Q-12-composed) — the only gate. (3) A Contradiction opens only when the re-measured reading confirms opposition: both Claims → contested (mechanical), pair becomes Juxtaposition material — met as material, never accusation (Q-15). (4) Dissolved candidates are ARCHIVEd with outcome as reason — anti-repetition. Cap: ≤2 live re-measures in the Queue (PROVISIONAL until slice-2 RESULTS). | Session decision (Clerk grill Q3) |
| Q-29 | The Clerk's write contract: op vocabulary is MINT / UPDATE / MERGE / SUPERSEDE (reason required) / ARCHIVE / KEEP, and an op list must be total over the readings swept — KEEP makes "judged redundant" distinguishable from "silently omitted". Status is never model-writable; transitions are mechanical (≥2 independent cites → evidenced; Propagation → user-attested; open Contradiction → contested). Rejected ops are logged with reason and skipped; the reading stays unprocessed for the next run. The wiki-side mirror of Q-1: invariants checked at one code boundary, never per-prompt discipline. | Session decision (Clerk grill Q2) |
| Q-28 | Claim minting is immediate: every harvested reading mints or updates a Claim on the next docket run, born `unconfirmed` with a single cite. No convergence gate, no pending pool, no on-demand minting — Q-21's status enum and computed coreness carry the epistemic honesty; sprawl is pruned by MERGE ops and lint, not prevented by gating. | Session decision (Clerk grill Q1) |
| Q-27 | Re-reading's primary purpose is self-recognition — evidence of who you were, watched for drift; orientation and material-mining are side effects, never the frame. The wiki shows evidence; the user stays the authority (anti-looping). This is what the temporal spine (immutable versions, diachronic contradictions) exists to serve. | Session decision (fog grill Q24) |
| Q-26 | Phone sittings are second-class-but-supported now: LAN browser onto the server behind the password gate; a phone-width styling pass only (media query, touch-sized quiet actions). No native app, no sync, no offline — the vault never leaves the server. | Session decision (fog grill Q23) |
| Q-25 | Vault custody: the interface is password-locked (LAN access control); personal folders (vault/, data/ personal content) stay gitignored from the project repo permanently. Backup rides the user's existing file-backup infrastructure — the vault is plain files by design (Q-3). Flag, not fight: gitignored is not backed up; the vault path must be inside a backup regime. | Session decision (fog grill Q22, user redirect) |
| Q-24 | The Coach's advice constitution: advice is a new agent speech act — Marginalia-class (never in Pieces), in-app only (Q-22 intact), choice-expanding by rule (options, never prescriptions — von Foerster's imperative), and guilt-free by construction (no streaks, no progress-shaming; dormancy is signal, never debt). | Session decision (fog grill Q21) |
| Q-23 | Every deterministic and agentic act is logged to an append-only Activity Log rendered on the interface. Transparency is structural, not optional: background autonomy (Q-22) is only trustworthy because its ledger is inspectable. | Session decision; gist-mining anti-gaslighting finding |
| Q-22 | Zero outbound contact, ever — no notifications, no reminders, no content-triggered pushes. The Clerk works its Docket continuously in the background; finished work waits visibly on the interface until the user walks in. Agent initiative ends at the app's edge. | Session decision (grill Q20, user redirect) |
| Q-21 | Claim anatomy: body (one agent sentence) + mandatory Range (Bateson — no context-free trait claims) + mandatory cites + Status enum (unconfirmed/evidenced/user-attested/contested) + read-log (looping-effect flag). No confidence numbers; coreness computed from the citation graph, never stored. | Session decision (grill Q19); Bateson, STyLE-OLM, Hacking |
| Q-20 | Sessions close with two moves inside the budget: the open door ("anything else we didn't touch?") whose answer is volunteered, highest-signal material; then the bookmark ("where should we pick up?") whose answer stocks the Queue as a user-declared thread outranking agent-minted candidates. Agent session summaries are forbidden at close — reading the agent's frame before harvest review contaminates the evidence (looping effect). | Session decision (grill Q18); Kvale structuring, Hacking looping |
| Q-19 | Every sitting declares a Target (self or Domain) alongside Mode. Domain instruments — Critical Decision Method, laddered grid, concept sorting — are first-class Protocols, co-equal with self instruments. The product is workshop and mirror; without a declared Target the inward default wins by gravity. | Session decision (drift correction) |
| Q-18 | Random means shuffle, never invent: Randomizer draws come only from (1) the user's own forgotten snippets (depth-stratified resurfacing) or (2) user-curated decks. The agent may not generate "random" questions — its inventions are correlated with its own model and are the source of too-far noise. No distance metric exists or is needed. | Session decision (grill Q17); Ashby uncorrelated-source, Fu et al. "too far" |
